# file:-deps como CÓPIA, não symlink/junction (npm >= 9).
# Necessário p/ @tootega/dase-mcp (file:../MCP): o vsce segue junctions e falha
# com "invalid relative path: extension/../MCP/..." ao empacotar o .vsix.
install-links=true
